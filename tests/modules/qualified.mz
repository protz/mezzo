open Pervasives
open list

val the_list = cons [int] (3, cons [int] (2, cons [int] (1, nil)))
val the_length = list2::length the_list

val the_list2 =
  let consi = cons [int] in
  consi (3, consi (2, consi (1, nil)))

val the_length2 = list2::length the_list2
