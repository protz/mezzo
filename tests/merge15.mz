open core
open absdefs

data pair a b = Pair { left: a; right: b }

val f3 (x: t, y: u): pair unknown unknown =
  if True then
    Pair { left = x; right = y }
  else
    Pair { left = y; right = x }
