val inc (x: int): int = x + 1

val y = inc 2

val rec f (x: int): int =
  f (inc x)

val count = cref 0

val counter (permission count: ref int): int =
  let x = rget count in
  rset (count, x + 1);
  x
