val x: ([a] a -> a, int) =
  builtin foobar

val y: [a] (a -> a, int) =
  builtin foobar

val exchange: [a, b] (consumes (a, b)) -> (b, a) =
  builtin foobar

val z: [a] ref a =
  builtin foobar

val w: ref ([a]a) =
  builtin foobar

val z2: [a] wref::wref a =
  builtin foobar

