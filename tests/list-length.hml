abstract int
fact duplicable int

data list a = Cons { head: a; tail: list a } | Nil

val rec length [a] (x: list a): int =
  match x with
  | Nil ->
      0
  | Cons { tail = tail } ->
      1 + length tail
  end

val zero = length Nil
