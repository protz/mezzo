(* forall can be hoisted out a pair *)

val x: [a] (a -> a, int)

(* forall can be pushed into a pair: this does not work, for now *)
(*
val y: ([a] a -> a, int)
*)
(* two forall's commute *)

val exchange: [b, a] (consumes (a, b)) -> (b, a)

(* forall commutes with ref -- this one is sound because ref
   is known to be covariant *)

val z: ref ([a] a)

val w: [a] ref a

val z2: wref::wref ([a] a)

