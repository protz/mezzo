(* There's an uninstanciated type variable hanging around, it's innocuous, but I
 * wanted to write down that specific case. *)
val length [a] (Pervasives::list a): int =
  fail

val the_length = length Pervasives::nil
