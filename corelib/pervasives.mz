(* Standard types *)

data choice a b =
  | Left  { contents: a }
  | Right { contents: b }

(* Standard types *)

(* Arithmetic *)

val max (x: int, y: int) : int =
  if x <= y then y else x

val min (x: int, y: int) : int =
  if x <= y then x else y

