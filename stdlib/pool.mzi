(* The type [pool a] is the type of a generic adopter, so to speak.
   It is able to adopt objects of type [a], for an arbitrary (but,
   for each pool, fixed) exclusive type [a]. *)

data mutable pool a =
  Pool adopts a

